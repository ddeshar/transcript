# Production stack for Transcript app (app + Postgres + Redis + optional pgAdmin)
# Persists data via named volumes (you can bind-mount to /srv/transcript if you prefer).
# Exposes only Traefik (80/443) and Portainer HTTPS (9443). App is behind Traefik.

name: ${PROJECT_NAME:-transcript}

services:
  app:
    # Build from local source on the server
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend
    # Optional tag for the built image
    image: ${IMAGE:-transcript-app:local}
    container_name: transcript-app
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /app
      AUDIO_STORAGE_PATH: /app/media/audio
      LOG_DIR: /app/logs
      SUBTITLE_DIR: /app/subtitles
    volumes:
      - ../media:/app/media
      - ../logs:/app/logs
      - ../subtitles:/app/subtitles
      - ../models:/app/models
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.trans.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.trans.entrypoints=websecure"
      - "traefik.http.routers.trans.tls.certresolver=le"
      - "traefik.docker.network=${PROJECT_NAME:-transcript}_proxy"
      # HTTP -> HTTPS redirect (permanent)
      - "traefik.http.routers.trans-web.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.trans-web.entrypoints=web"
      - "traefik.http.routers.trans-web.middlewares=redirect-https@docker"
      - "traefik.http.middlewares.redirect-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-https.redirectscheme.permanent=true"
      # Service port inside the container
      - "traefik.http.services.trans.loadbalancer.server.port=8000"
    networks:
      - default
      - proxy
    # Healthcheck that doesn't require curl to be present
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python - <<'PY'\nimport urllib.request,sys\ntry:\n  urllib.request.urlopen('http://localhost:8000/health',timeout=3)\n  print('OK')\nexcept Exception as e:\n  print(e); sys.exit(1)\nPY"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    # Hardening
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"   # ensure your Dockerfile creates uid:gid 1000 and fixes ownership
    read_only: true
    tmpfs:
      - /tmp:size=64m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 6g
        reservations:
          cpus: "1.0"
          memory: 3g

  # Portainer for Docker management (optional; restrict at network/firewall level)
  portainer:
    image: portainer/portainer-ce:latest
    container_name: transcript-portainer
    restart: unless-stopped
    ports:
      # Prefer HTTPS only
      - "9443:9443"
    command:
      - --http-disabled
      - --assets-dir=/public
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  db:
    image: postgres:15-alpine
    container_name: transcript-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-seminar_platform}
      POSTGRES_USER: ${POSTGRES_USER:-seminar_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-seminar_pass}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ../scripts/init_db.sql:/docker-entrypoint-initdb.d/init_db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-seminar_user} -d ${POSTGRES_DB:-seminar_platform}"]
      interval: 10s
      timeout: 5s
      retries: 5
    shm_size: "256m"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 2g

  redis:
    image: redis:7-alpine
    container_name: transcript-redis
    restart: unless-stopped
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --appendfsync
      - everysec
      - --requirepass
      - ${REDIS_PASSWORD:?set_in_.env}
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  # Optional: pgAdmin for DB inspection (no exposed ports by default)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: transcript-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@example.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin123}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    volumes:
      - pgadmin_data:/var/lib/pgadmin
      - ../scripts/servers.json:/pgadmin4/servers.json:ro
    depends_on:
      db:
        condition: service_healthy
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Traefik reverse proxy with automatic Let's Encrypt (TLS)
  traefik:
    image: traefik:v2.11
    container_name: transcript-traefik
    restart: unless-stopped
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      # If you set this, you can omit the app's traefik.docker.network label:
      # - --providers.docker.network=${PROJECT_NAME:-transcript}_proxy
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.le.acme.email=${TRAEFIK_ACME_EMAIL}
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.le.acme.httpchallenge=true
      - --certificatesresolvers.le.acme.httpchallenge.entrypoint=web
      - --api.dashboard=false
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Mount a single acme.json file so 0600 perms are preserved
      - ./traefik/letsencrypt/acme.json:/letsencrypt/acme.json
    networks:
      - proxy
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  db_data:
  redis_data:
  pgadmin_data:
  portainer_data:

networks:
  default:
    driver: bridge
    name: ${PROJECT_NAME:-transcript}_default
  proxy:
    driver: bridge
    name: ${PROJECT_NAME:-transcript}_proxy
